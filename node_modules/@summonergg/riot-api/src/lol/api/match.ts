import { api } from "../../modules/limiter";
import { log } from "chalk-logging";

interface MatchQuery {
  matchId?: string;
  accountId?: string;
  champion?: string;
  queue?: string;
  season?: string;
  endTime?: string;
  beginTime?: string;
  endIndex?: string;
  beginIndex?: string;
  tournamentCode?: string;
  
}

export class Match implements MatchDTO {
  gameId?: number;
  participantIdentities?: ParticipantIdentityDTO[];
  queueId?: number;
  gameType?: string;
  gameDuration?: number;
  teams?: TeamStatsDTO[];
  platformId?: string;
  gameCreation?: number;
  seasonId?: number;
  gameVersion?: string;
  mapId?: number;
  gameMode?: string;
  participants?: ParticipantDTO[];

  static async get(data: MatchQuery): Promise<Match> {
    return new Promise(async (resolve, reject) => {
      var endpoint: string;

    })
  }
}