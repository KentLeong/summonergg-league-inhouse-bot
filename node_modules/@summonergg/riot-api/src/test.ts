import { League } from "./lol"
import { log } from "chalk-logging";
import { api } from "./modules/limiter";
import { Summoner } from "./lol"
import axios from "axios";

(async () => {
  
  // var league = await League.get({summonerId: "C-dE3eK53OefJnIPW58zvr2Qjo2fiKxFG_aA5zip9XjT7MtP"});
  // console.log(league)
  // var summoner: any = await Summoner.get({name: "pass", region: "na"});
  var summoner: any = await Summoner.get({name: "pass", region: "na"})
  var leagues = await League.get({summonerId: summoner.id});
})();