import Discord from "discord.js";

declare global {
  interface DiscordMessage extends Discord.Message {
    server?: Guild;
    args?: string[];
    auth?: auth;
  }

  interface DiscordClient extends Discord.Client {

  }

  type TranslateResponse = (id: number, args?: Record<string, any>) => undefined;
  type regions = "na" | "kr";
  type UserAuth = "leong" | "owner" | "mod" | "hoster" | "member" | "none";
  type Translater = (msg: DiscordMessage, type: string) => (id: number, args: any) => void;
  
  type apiOptions = {
    msg?: DiscordMessage,
    client?: DiscordClient
  }

  interface clientType extends apiOptions {

  }

  interface Playerlol {
    account: any,
    discordId: string,
    ready: boolean,
    roles: string[],
    gold: number,
    level: number,
    experience: number
  }
}