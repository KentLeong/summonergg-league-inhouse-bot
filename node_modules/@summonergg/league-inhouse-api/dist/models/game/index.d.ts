import { API } from "../api";
import { User } from "../../";
import Discord from "discord.js";
export declare class Game extends API {
    opt?: apiOptions;
    host: string;
    players: Playerlol[];
    _id: string;
    state: string;
    map: string;
    draft: string;
    modes: Array<string>;
    region: any;
    server: string;
    blueTeam: Playerlol[];
    redTeam: Playerlol[];
    championPool: Array<object>;
    restrictor: Array<string>;
    bot: string;
    name: string;
    done: boolean;
    message: string;
    channel: string;
    maxPlayers: number;
    ranked: boolean;
    createdAt: Date;
    res?: TranslateResponse;
    msg?: DiscordMessage;
    clientType?: clientType;
    guild?: Discord.Guild;
    client?: Discord.Client;
    constructor(opt?: apiOptions);
    displayLobby(): Promise<void>;
    addPlayer(user: User): Promise<boolean>;
    playerReady(discordId: string): Promise<boolean>;
    removePlayer(user: User): Promise<boolean>;
    cancel(): Promise<void>;
    isReady(): Promise<boolean>;
    sortPlayerELO(): Promise<void>;
    startLobby(): Promise<void>;
    awaitResponse(captains: Record<string, any>, available: Set<string>, players: Playerlol[], state: string): Promise<void>;
}
