import { User, Token, Guild } from "../../";
import { Summoner } from "@summonergg/riot-api";
import { Game } from "../game";
import Discord from "discord.js";
declare type userOptions = "hasSummoner" | "isNotInGame";
export declare class DiscordAPI {
    opt?: apiOptions;
    res?: TranslateResponse;
    region?: regions;
    name?: string;
    summoner?: Summoner;
    user?: User;
    msg?: DiscordMessage;
    client?: DiscordClient;
    args?: string[];
    discordId?: string;
    clientType?: clientType;
    guild?: Discord.Guild;
    server?: Guild;
    channel?: string;
    constructor(opt?: apiOptions);
    signupValid(): Promise<boolean>;
    summonerTaken(): Promise<boolean>;
    createToken(): Promise<void>;
    getToken(): Promise<Token>;
    compareIcons(summoner: Summoner, token: Token): Promise<boolean>;
    findOrCreateUser(): Promise<User>;
    findUser(options?: Array<userOptions>): Promise<User>;
    findGame(options?: Record<string, string>): Promise<Game>;
    hostGame(host: User, options?: any): Promise<Game>;
}
export {};
