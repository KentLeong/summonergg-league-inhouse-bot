import { API } from "../api";
export declare class Token extends API {
    opt?: apiOptions;
    discordId: string;
    summonerId: string;
    region: string;
    profileIconId: number;
    key: string;
    res?: TranslateResponse;
    msg?: DiscordMessage;
    constructor(opt?: apiOptions);
    generateNew(id: number): Promise<void>;
}
