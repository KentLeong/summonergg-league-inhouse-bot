import { API } from "../api";
declare type GuildRoles = "mod" | "hoster" | "member" | string;
declare type GuildStats = "level" | "games" | "experience" | string;
declare type GuildChannels = "category" | "verify" | "host" | "hosts" | string;
export declare class Guild extends API {
    opt?: apiOptions;
    id: string;
    channels: Record<GuildChannels, string | string[]>;
    stats: Record<GuildStats, number | string>;
    roles: Record<GuildRoles, string>;
    open: boolean;
    language: string;
    prefix: string;
    region: string;
    res?: TranslateResponse;
    msg?: DiscordMessage;
    constructor(opt?: apiOptions);
}
export {};
