export const translate = (msg: DiscordMessage, model: string): TranslateResponse => {
  if (!msg) return;
  var language = msg.server.language;
  var translater = require(`../models/${model}/translate/${language}`)[language](msg);
  return translater
}