import dotenv from "dotenv";
dotenv.config();
import axios from "axios";

export { Game } from "./models/game";
export { Guild } from "./models/guild";
export { Token } from "./models/token";
export { User } from "./models/user";
export { DiscordAPI } from "./models/discord";

const IP = process.env.LEAGUE_INHOUSE_CLIENT_IP;
const PORT = process.env.LEAGUE_INHOUSE_CLIENT_PORT;

export const client = axios.create({
  baseURL: `http://${IP}:${PORT}/api`
})
